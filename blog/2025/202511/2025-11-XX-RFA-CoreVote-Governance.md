# 核心治理合约进入社区审计阶段

近期，教链完成了核心投票权代币合约及其治理合约的初步开发。本开发乃是基于uniswap治理合约进行改造移植和二次开发的成果。

待审计的代码在PR#3 [Request For Audit] first revision of CoreVote and its Governor Alpha https://github.com/Jouleverse/governance/pull/3 。主要包含两个文件：CoreVote.sol 为投票权代币合约。CoreVoteGovernorAlpha.sol为其对应的治理合约。请社区的伙伴们多多进行代码审查和安全审计，多一双眼睛多一层把关，尽力保障核心合约的正确性、安全性。

## 一、主要功能介绍

### CoreVote.sol

`CoreVote.sol` 是一个**投票权代币合约**，主要功能如下：

#### 核心特性

##### 1. **不可转移的治理代币**
- 代币名称：Core Vote (CVOTE)
- **转账功能被禁用** - 只能通过铸造产生，不能交易转移
- 专为治理投票设计，防止投票权买卖

##### 2. **委托投票机制**
- 代币持有者可以将投票权委托给其他地址
- 支持直接委托和离线签名委托
- 投票权随代币余额自动调整

##### 3. **铸币和燃烧控制**
- 只有指定的 `minter` 地址可以铸造新代币
- 支持燃烧不活跃账户的代币
- 自动处理委托投票权的更新

##### 4. **历史投票权追踪**
- 使用检查点(Checkpoint)记录每个区块的投票权变化
- 可以查询任意历史区块的投票权余额
- 防止临时转移代币操纵投票

##### 5. **离线签名支持**
- 支持 EIP-712 标准签名
- 无需交易即可进行委托和授权
- 提升用户体验和 Gas 效率

#### 主要应用场景

##### 🗳️ **DAO 治理**
- 代币代表投票权
- 用于协议升级、参数调整等治理决策
- 防止投票权集中在少数人手中

##### ⚡ **快速投票**
- 通过委托实现专业治理
- 普通用户可以将投票权委托给专业人士
- 提高治理效率和专业性

##### 🔒 **防操纵机制**
- 基于历史快照的投票权查询
- 防止在投票前后快速买卖代币
- 确保投票的公平性

#### 技术特点

- **Gas 优化**: 使用 `uint96` 节省存储空间
- **安全可靠**: 完整的溢出检查和权限控制
- **标准兼容**: 部分实现 ERC-20 接口，支持钱包显示

这个合约是治理系统的核心组件，为去中心化自治组织(DAO)提供安全、高效的投票权管理机制。

### CoreVoteGovernorAlpha.sol

`CoreVoteGovernorAlpha.sol` 是一个**去中心化治理合约**，主要功能如下：

#### 核心功能

##### 1. **提案管理**
- 创建治理提案（修改协议参数、资金使用等）
- 每个提案包含多个执行动作（目标合约、函数调用、参数）
- 防止提案泛滥：每个地址同时只能有一个活跃提案

##### 2. **投票系统**
- 基于代币权重的投票机制
- 支持赞成票和反对票
- 使用历史投票权快照，防止临时操纵
- 支持直接投票和离线签名投票

##### 3. **提案生命周期管理**
```solidity
ProposalState {
    Pending,    // 投票尚未开始
    Active,     // 投票进行中
    Canceled,   // 已取消
    Defeated,   // 投票失败
    Succeeded,  // 投票通过
    Queued,     // 已排队等待执行
    Expired,    // 执行期限已过
    Executed    // 已执行
}
```

##### 4. **时间锁集成**
- 通过 Timelock 合约延迟执行通过的提案
- 提供撤销提案的机会（安全机制）
- 防止恶意提案立即生效

#### 关键参数

##### 📊 **投票门槛**
- **提案门槛**：总供应量的 1/3（防止垃圾提案）
- **法定人数**：总供应量的 2/3（确保代表性）
- **投票延迟**：1 个区块（防止抢先交易）
- **投票周期**：约7天（40,320个区块）

##### ⚡ **执行流程**
1. **创建提案** → 2. **投票期** → 3. **排队等待** → 4. **时间锁延迟** → 5. **执行**

#### 安全机制

##### 🔒 **防滥用保护**
- 一个地址同时只能有一个活跃提案
- 基于历史区块的投票权快照
- 提案取消条件：提案人投票权低于门槛

##### 🛡️ **多重检查**
- 提案参数完整性验证
- 状态机确保正确流程
- 签名验证防止伪造投票

#### 应用场景

##### 🏛️ **协议治理**
- 参数调整（利率、手续费等）
- 国库资金使用
- 协议升级决策

##### 🔄 **生态系统管理**
- 新增支持资产
- 修改奖励机制
- 社区基金分配

这个合约与 `CoreVote.sol` 配合，构成了完整的去中心化治理系统，让投票权代币持有者能够真正参与协议决策。

## 二、改造要点介绍

### CoreVote.sol

对比 `CoreVote.sol` 和 `Uni.sol`，`CoreVote.sol` 主要修改了以下关键点：

#### 🚫 **核心功能修改**

##### 1. **禁止代币转移**
```solidity
// CoreVote - 转账被禁用
function transfer() external pure returns (bool) {
    revert("CoreVote::transfer: prohibited");
}

// Uni - 正常转账功能
function transfer() external returns (bool) {
    _transferTokens(msg.sender, dst, amount);
    return true;
}
```

##### 2. **初始供应量**
```solidity
// CoreVote - 零初始供应
uint public totalSupply = 0;

// Uni - 10亿初始供应  
uint public totalSupply = 1_000_000_000e18;
```

#### ⚙️ **铸币机制简化**

##### 3. **移除铸币限制**
```solidity
// CoreVote - 无时间限制和数量限制
function mint() external {
    // 直接铸币，无额外检查
}

// Uni - 严格的铸币控制
function mint() external {
    require(block.timestamp >= mintingAllowedAfter, "minting not allowed yet");
    require(amount <= (totalSupply * mintCap) / 100, "exceeded mint cap");
    mintingAllowedAfter = block.timestamp + minimumTimeBetweenMints;
}
```

#### 🔥 **新增燃烧功能**

##### 4. **代币燃烧机制**
```solidity
// CoreVote 新增
function burn(address account, uint rawAmount) external {
    // 只有minter可以燃烧不活跃账户的代币
    _moveDelegates(delegates[account], address(0), amount);
}

function burnMax(address account) external {
    // 燃烧全部代币
}
```

#### 🔄 **技术升级**

##### 5. **Solidity 版本升级**
```solidity
// CoreVote - 现代版本
pragma solidity ^0.8.30;

// Uni - 旧版本  
pragma solidity ^0.5.16;
```

##### 6. **语法更新**
```solidity
// CoreVote - 现代语法
if (rawAmount == type(uint).max) {
    amount = type(uint96).max;
}
require(block.timestamp <= deadline);

// Uni - 旧语法
if (rawAmount == uint(-1)) {
    amount = uint96(-1);
}
require(now <= deadline);
```

#### 🎯 **设计理念差异**

| 特性 | CoreVote | Uni |
|------|----------|-----|
| **代币用途** | 纯治理投票 | 治理+可交易 |
| **转移性** | ❌ 不可转移 | ✅ 可转移 |
| **供应机制** | 从零开始铸造 | 10亿初始供应 |
| **铸币控制** | 简单权限控制 | 严格时间+数量限制 |
| **燃烧功能** | ✅ 有 | ❌ 无 |

#### 总结

`CoreVote.sol` 的核心修改是**将代币从可交易资产转变为纯粹的治理工具**：
- **禁止转移** → 防止投票权买卖
- **零初始供应** → 完全通过铸造分配
- **简化铸币** → 更灵活的治理权分配
- **新增燃烧** → 清理不活跃投票权

这些修改使 `CoreVote` 更适合作为**专业的DAO治理代币**，专注于投票权管理而非金融属性。

### CoreVoteGovernorAlpha.sol

对比 `CoreVoteGovernorAlpha.sol` 和 Uniswap 的 `GovernorAlpha.sol`，主要修改了以下关键点：

#### 📊 **治理参数动态化**

##### 1. **动态投票门槛**
```solidity
// CoreVoteGovernorAlpha - 基于总供应量比例
function quorumVotes() public view returns (uint) { 
    return cvote.totalSupply() / 3 * 2; // 2/3 of total
}

function proposalThreshold() public view returns (uint) { 
    return cvote.totalSupply() / 3; // 1/3 of total
}

// Uniswap GovernorAlpha - 固定数值
function quorumVotes() public pure returns (uint) { 
    return 40_000_000e18; // 固定4千万UNI
}

function proposalThreshold() public pure returns (uint) { 
    return 10_000_000e18; // 固定1千万UNI
}
```

#### 🔧 **技术架构升级**

##### 2. **Solidity 版本升级**
```solidity
// CoreVoteGovernorAlpha - 现代版本
pragma solidity ^0.8.30;

// Uniswap GovernorAlpha - 旧版本
pragma solidity ^0.5.16;
```

##### 3. **数据结构优化**
```solidity
// CoreVoteGovernorAlpha - 分离receipts映射
mapping (uint => mapping (address => Receipt)) public receipts;

// Uniswap GovernorAlpha - receipts在Proposal结构体内
struct Proposal {
    // ...
    mapping (address => Receipt) receipts;
}
```

#### 🛠️ **语法和功能更新**

##### 4. **现代语法适配**
```solidity
// CoreVoteGovernorAlpha
timelock.executeTransaction{value: proposal.values[i]}(...);
require(block.timestamp <= deadline);

// Uniswap GovernorAlpha  
timelock.executeTransaction.value(proposal.values[i])(...);
require(now <= deadline);
```

##### 5. **算术运算简化**
```solidity
// CoreVoteGovernorAlpha - 使用内置运算
proposal.forVotes += votes;
block.number + votingDelay()

// Uniswap GovernorAlpha - 自定义安全函数
proposal.forVotes = add256(proposal.forVotes, votes);
add256(block.number, votingDelay())
```

#### 🔄 **提案创建逻辑**

##### 6. **存储优化**
```solidity
// CoreVoteGovernorAlpha - 直接在存储中创建
Proposal storage newProposal = proposals[proposalCount];
newProposal.id = proposalCount;
// 直接赋值各个字段...

// Uniswap GovernorAlpha - 内存创建后复制
Proposal memory newProposal = Proposal({
    id: proposalCount,
    // 结构体初始化...
});
proposals[newProposal.id] = newProposal;
```

#### 📈 **核心设计差异**

| 特性 | CoreVoteGovernorAlpha | Uniswap GovernorAlpha |
|------|---------------------|---------------------|
| **提案门槛** | 动态比例(总供应量1/3) | 固定数值(1千万UNI) |
| **法定人数** | 动态比例(总供应量2/3) | 固定数值(4千万UNI) |
| **Solidity版本** | 0.8.30 | 0.5.16 |
| **数据结构** | 分离receipts映射 | 嵌套receipts映射 |
| **代币集成** | CVote | UNI |

#### 🎯 **修改总结**

`CoreVoteGovernorAlpha.sol` 的主要修改集中在：

1. **治理参数动态化** - 适应可变的总供应量
2. **技术栈现代化** - 升级到Solidity 0.8+，使用新语法
3. **数据结构优化** - 解决映射在内存中的限制问题
4. **代码简化** - 利用新版本的语言特性

这些修改使合约更适合与 `CoreVote.sol`（零初始供应、可铸造代币）配合使用，同时保持了与原始Uniswap治理合约相同的核心治理逻辑和安全保证。

## 三、运营思路

这套Core治理体系的运营思路如下：

1. 由 `人才发展部` 负责定期（季度或半年度）审查所有Core成员（核心贡献者）的活跃度，出具活跃名单和非活跃名单并在github及《焦耳通讯》等处公示7天
2. 公示期满无异议，由 `核心多签人` 负责燃烧掉不再活跃的Core成员的投票权代币
3. 新加入、已完成KYC的Core成员，须获得第一次PoWh工作量贡献链上奖励后，才能由 `人才发展部` 公示加入活跃Core成员名单，并由 `核心多签人` 赋予新的投票权（1人1票）
4. 任何拥有投票权的Core成员均可以向社区收集投票权委托支持，一旦集齐超过投票权总量的1/3，就可以在链上正式发起提案
5. 拟提案者在征集社区支持之前，需要先在github上通过 提issue、提PR 的流程，把提案内容整理并公示
6. 链上投票一旦通过（赞成票超过投票权总量的2/3），提案正式生效，具备法定效力，各执行团队有义务遵照执行；对于提案中内嵌的链上执行指令（比如从金库划拨预算等），将被智能合约无条件执行
